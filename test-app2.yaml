apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-web-app2
  labels:
    app: test-web-app2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: test-web-app2
  template:
    metadata:
      labels:
        app: test-web-app2
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: test-web-app-service2
spec:
  selector:
    app: test-web-app2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-app-ingress2
spec:
  ingressClassName: nginx
  rules:
  - host: app2.k8s-ingress1.lan
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test-web-app-service2
            port:
              number: 80