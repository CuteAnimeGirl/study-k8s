protocolHttp: true
extraArgs:
  - --enable-insecure-login
  - --insecure-bind-address=0.0.0.0
  - --namespace=kubernetes-dashboard
  - --insecure-port=9090

service:
  port: 80
  targetPort: http

kong:
  proxy:
    http:
      enabled: true
      servicePort: 80
    tls:
      enabled: false