protocolHttp: true
extraArgs:
  - --enable-insecure-login
service:
  port: 80
  targetPort: http

kong:
  enabled: true
  proxy:
    type: ClusterIP
    http:
      enabled: true
      servicePort: 80
      containerPort: 8000
    tls:
      enabled: false